import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {display: 'flex', flexDirection: 'row', paddingTop: 10},
  prompt: {color: 'black', columnGap: 5},
  lintText: {
    color: 'black',
    fontWeight: 'bold',
    textDecorationLine: 'underline',
  },
});
